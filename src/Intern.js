const Employee = require('./Employee.js')

class Intern extends Employee{
    constructor(school) {
        super(ename, id, email);
        this.school = school;
    }

    getRole() {
        return "Intern"
    }

    getSchool() {

    }
};
